# Kubernetes (k8s) Cheat Sheet

A quick-reference guide for commonly used `kubectl` commands, debugging steps, and manifest snippets.

## Common Aliases
```bash
alias k="kubectl"
alias kgp="kubectl get pods"
alias kgs="kubectl get svc"
alias kgn="kubectl get nodes"
